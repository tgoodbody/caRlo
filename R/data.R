#' GEDI data
#'
#' This is an `sf` object containing the spatial data for 1000 GEDI samples in a study area.
#'
#' @format `sf` object.
#' @source This data was collected by the GEDI mission
#' @keywords datasets
"gedi"

#' Samples from monte_carlo()
#'
#' This is an `sf` object containing subsampled plots.
#'
#' @format `sf` object.
#' @keywords datasets
"samples"

#' Sample plot data
#'
#' This is an `sf` object containing the spatial data for 1000 samples.
#'
#' @format `sf` object.
#' @keywords datasets
"plots"

#' Statistics from `samples`
#'
#' This is a nested dataframe object containing the statistics dervied from `samples`.
#'
#' @format `sf` object.
#' @keywords datasets
"stats"
